package com.agilexp.repository;

import com.agilexp.model.SolutionSourceCode;
import org.springframework.data.repository.CrudRepository;

public interface SolutionSourceCodeRepository extends CrudRepository<SolutionSourceCode, Long> {
}
